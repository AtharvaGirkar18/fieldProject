<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Attendance Statistics</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-indigo-50"
  >
    <%- include("../partials/navbar", currUser, teacher) %>

    <div class="container mx-auto px-4 py-8">
      <div
        class="flex flex-col md:flex-row md:items-center md:justify-between mb-8 gap-3"
      >
        <div>
          <p class="text-sm text-gray-500">Teacher</p>
          <h1 class="text-3xl font-bold text-gray-800">
            <%= teacher.username %>
          </h1>
        </div>
        <div class="flex gap-3">
          <a
            href="/coordinator/teacherDetails/<%= teacher._id %>"
            class="px-4 py-2 rounded-lg bg-white border border-gray-200 shadow-sm text-gray-700 hover:bg-gray-50"
            >Back to today&apos;s report</a
          >
          <a
            href="/coordinator/attendance/<%= teacher._id %>/photos"
            class="px-4 py-2 rounded-lg bg-blue-600 text-white shadow-sm hover:bg-blue-700"
            >View attendance photos</a
          >
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <div class="bg-white rounded-2xl shadow-md border border-gray-100 p-6">
          <p class="text-sm text-gray-500">Teacher Attendance</p>
          <h3 class="text-3xl font-bold text-green-600">
            <%= teacherAttendance.present %> / <%= teacherAttendance.total %>
          </h3>
          <p class="text-xs text-gray-500 mt-1">days present</p>
        </div>
        <div class="bg-white rounded-2xl shadow-md border border-gray-100 p-6">
          <p class="text-sm text-gray-500">Total Students</p>
          <h3 class="text-3xl font-bold text-blue-600">
            <%= teacher.students ? teacher.students.length : 0 %>
          </h3>
        </div>
        <div class="bg-white rounded-2xl shadow-md border border-gray-100 p-6">
          <p class="text-sm text-gray-500">Total Lectures</p>
          <h3 class="text-3xl font-bold text-indigo-600">
            <%= reports ? reports.length : 0 %>
          </h3>
        </div>
      </div>

      <div class="bg-white rounded-2xl shadow-md border border-gray-100 p-6">
        <div class="flex items-center justify-between mb-4">
          <h2 class="text-lg font-bold text-gray-800">Student Attendance</h2>
          <span class="text-sm text-gray-500"
            ><%= studentAttendanceData.length %> students</span
          >
        </div>
        <div class="overflow-x-auto">
          <table class="min-w-full">
            <thead>
              <tr class="border-b">
                <th
                  class="text-left py-3 px-4 text-sm font-semibold text-gray-600"
                >
                  Student
                </th>
                <th
                  class="text-center py-3 px-4 text-sm font-semibold text-gray-600"
                >
                  Present
                </th>
                <th
                  class="text-center py-3 px-4 text-sm font-semibold text-gray-600"
                >
                  Total
                </th>
                <th
                  class="text-center py-3 px-4 text-sm font-semibold text-gray-600"
                >
                  Attendance %
                </th>
                <th
                  class="text-center py-3 px-4 text-sm font-semibold text-gray-600"
                >
                  Status
                </th>
              </tr>
            </thead>
            <tbody>
              <% studentAttendanceData.forEach((data) => { %>
              <tr class="border-b hover:bg-gray-50">
                <td class="py-3 px-4">
                  <div class="flex items-center gap-3">
                    <div
                      class="w-10 h-10 rounded-full bg-gradient-to-br from-blue-400 to-indigo-500 flex items-center justify-center text-white font-bold"
                    >
                      <%= data.student.name ?
                      data.student.name.charAt(0).toUpperCase() : 'S' %>
                    </div>
                    <span class="text-gray-800 font-medium"
                      ><%= data.student.name %></span
                    >
                  </div>
                </td>
                <td class="text-center py-3 px-4 font-semibold text-gray-700">
                  <%= data.presentCount %>
                </td>
                <td class="text-center py-3 px-4 font-semibold text-gray-700">
                  <%= data.totalClasses %>
                </td>
                <td
                  class="text-center py-3 px-4 text-lg font-bold <%= data.percentage >= 75 ? 'text-green-600' : data.percentage >= 50 ? 'text-yellow-600' : 'text-red-600' %>"
                >
                  <%= data.percentage %>%
                </td>
                <td class="text-center py-3 px-4">
                  <% if (data.percentage >= 75) { %>
                  <span
                    class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800"
                    >Excellent</span
                  >
                  <% } else if (data.percentage >= 50) { %>
                  <span
                    class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800"
                    >Average</span
                  >
                  <% } else { %>
                  <span
                    class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-red-100 text-red-800"
                    >Low</span
                  >
                  <% } %>
                </td>
              </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </body>
</html>
